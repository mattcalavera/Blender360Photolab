# 360 PhotoLab v0.36.0-beta

This beta release renames the project to **360 PhotoLab** and prepares the add-on for Blender Extensions review with a cleaner public name, updated manifest and standard add-on registration flow.

360 PhotoLab is an add-on for Blender that reframes equirectangular 360° still photos into conventional photographic exports.

## Important changes

### Renamed extension

The public extension name is now:

```text
360 PhotoLab
```

The technical manifest ID is:

```text
photo360_lab
```

This avoids using the Blender trademark as part of the product name while still describing the project correctly as an add-on for Blender.

### Cleaned registration flow

The development-only helper `unregister_old_classes_and_props()` has been removed from the public extension package.

`register()` now follows a standard add-on flow and no longer performs legacy cleanup/versioning actions during add-on activation.

## Main features

- Reframe equirectangular 360° still photos inside Blender.
- Automatic scene setup after loading an image.
- Automatic Rendered Camera View for the main framing workflow.
- Projection modes:
  - **RET** — Rectilinear / perspective view.
  - **CIL** — Cylindrical projection.
  - **CLV** — Level cylindrical projection, designed to reduce horizon bending by treating pitch as a vertical shift.
- Comparative preview layout:
  - CIL and CLV on the top row.
  - RET centered below.
- Orthographic preview view without the semi-transparent camera frame.
- Direct export without relying on Blender's normal render output pipeline.
- Export formats:
  - PNG 8-bit lossless;
  - TIFF 16-bit lossless;
  - high-quality JPEG.
- `.cube` LUT support.
- LUTs are automatically applied to previews and final exports when loaded.
- Print-oriented resolution tools:
  - preset output sizes;
  - custom output resolution;
  - estimated print size at 150 DPI and 300 DPI;
  - native/detail-aware resolution estimation for the current framing.
- Robust equirectangular sampling with bilinear interpolation.
- Seam handling for the ±180° equirectangular boundary.

## Installation

For Blender 4.2+ / 5.x, install the add-on as an extension package.

Download the ZIP file attached to this release under **Assets**:

```text
360PhotoLab-v0.36.0-beta-extension.zip
```

Do **not** install GitHub's automatically generated:

```text
Source code (zip)
Source code (tar.gz)
```

Those files are repository archives and are not the Blender extension package.

Then in Blender:

1. Open **Edit > Preferences**.
2. Go to **Extensions**.
3. Choose **Install from Disk**.
4. Select the ZIP file downloaded from the release assets.
5. Enable **360 PhotoLab**.
6. Open the **3D Viewport Sidebar** and use the **360 PhotoLab** panel.

## Known limitations

This is a beta release.

- The add-on is focused on still images, not video reframing.
- CLV helps reduce horizon curvature, but it cannot remove every form of projection distortion.
- Very wide fields of view may still show visible projection stretching.
- TIFF export is intended as a high-quality master format, but file sizes can be large.
- LUT handling is practical and export-oriented; full color-management workflows may still require external grading tools.
- Performance depends on source image resolution, output resolution and available system memory.

## License

360 PhotoLab is released under the GPL-3.0-or-later license.

## Notes

Feedback, bug reports and test images are welcome.
